#include <iostream>
#include <algorithm>
using namespace std;


int main()
{
	long long int a , b, c, d, x;
	cin >> a >> b >> c >> d;
	x = (a * b) - (c * d);
	cout << "Difference = " << x << endl;
	return 0;
}